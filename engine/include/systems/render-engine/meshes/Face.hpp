#pragma once

#include <glm/glm.hpp>

namespace NAISE {
namespace RenderCore {

struct Triangle {
  Triangle(vec3 v1, vec3 v2, vec3 v3, vec3 normal) {};
};

struct Face {
	Face(vec3 v1, vec3 v1, vec3 v1, vec3 v1) {};
};

}
}
